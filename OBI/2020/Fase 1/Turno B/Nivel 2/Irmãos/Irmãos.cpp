#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int maisNovo, otavio;
    cin >> maisNovo >> otavio;

    int velho = 2*otavio - maisNovo;

    cout << velho;

    return 0;
}