#include <bits/stdc++.h>
using namespace std;

int n;
int maxi = 0;
int tab[2][510];

void busca(int lin, int col, int linha[]){
    
}
int calc(int lin, int linha[]){
    maxi
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n;
    int m;
    cin >> m;
    for(int i = 0; i < m; i++)
        cin >> tab[0][i];
    cin >> m;
    for(int i = 0; i < m; i++)
        cin >> tab[1][i];

    busca(0,0, tab[0]);
    cout << maxi;
    return 0;
}