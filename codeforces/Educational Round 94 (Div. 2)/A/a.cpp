#include <bits/stdc++.h>

#define endl "\n"
using namespace std;

int main(){
    freopen("in","r",stdin);
    freopen("out","w",stdout);

    ios::sync_with_stdio(0);
    cin.tie(NULL);

    int t;
    cin >> t;

    for(int i = 1; i <= t; i++){
        int n;
        cin >> n;

        int linha[2*n-1];
        for(int j = 0; j <= 2*n-1; j++)
            cin >> linha[j];
        
        int ini = 1;
        int fim = n;
        int cont = 0;
        queue<int> answ;
        while(ini < n && fim < 2*n-1){
            answ.push()
        }
    }

    return 0;
}